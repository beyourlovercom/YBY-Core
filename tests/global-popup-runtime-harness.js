const fs = require("fs");
const js = fs.readFileSync("public/js/yby-global-popup.js", "utf8");
if (!js.includes("window.YBYPopup")) throw new Error("Popup runtime must expose YBYPopup");
if (!js.includes('type === "inquiry"') || !js.includes("window.YBYInquiry.open")) throw new Error("Inquiry popup ownership must delegate to YBYInquiry.open");
if (!js.includes('closest("[data-yby-popup-open]")')) throw new Error("Global Popup must own data-yby-popup-open triggers");
if (!js.includes('matches("[data-yby-subscribe-contract]")')) throw new Error("Global Popup must own the subscribe contract");
if (js.includes("data-yby-inquiry-trigger") || js.includes("data-yby-quote-trigger")) throw new Error("Global Popup must not claim generic inquiry or quote triggers");
if (!js.includes("context") || !js.includes("page_profile") || !js.includes("trigger_source")) throw new Error("Popup context contract missing");
if (!js.includes("data-yby-popup-close") || !js.includes('event.key === "Escape"') || !js.includes("function close")) throw new Error("Popup close and Escape behavior missing");
if (!js.includes("yby_popup_preview") || !js.includes("DOMContentLoaded")) throw new Error("Preview navigation must use the production popup runtime");
if (!js.includes('hash.indexOf("yby-popup=")') || !js.includes('data-yby-source", "admin_preview"')) throw new Error("Preview navigation must create a production-renderer trigger context");
if (!js.includes("YBYInquiry.close")) throw new Error("Global Popup must delegate Inquiry close to YBYInquiry");
if (!js.includes("delegatedInquiryModal")) throw new Error("Global Popup must track the actual delegated Inquiry modal");
if (js.includes('YBYInquiry.close(null')) throw new Error("Global Popup must never close Inquiry with null");
if (!js.includes("closeDelegatedInquiry")) throw new Error("Global Popup must clear or close delegated Inquiry explicitly");
if (!js.includes("close(); closeDelegatedInquiry")) throw new Error("Opening Subscribe must close delegated Inquiry first");
if (!js.includes("inquiryModal = window.YBYInquiry.open")) throw new Error("Inquiry modal return value must be tracked");
if (!js.includes("event.key !== \"Tab\"")) throw new Error("Subscribe must trap Tab focus");
if (!js.includes("event.key === \"Escape\"")) throw new Error("Subscribe must close on Escape");
if (!js.includes("document.body.classList.add(\"yby-popup-open\")")) throw new Error("Subscribe must lock body scroll while open");
console.log("PASS global-popup-runtime-harness");
