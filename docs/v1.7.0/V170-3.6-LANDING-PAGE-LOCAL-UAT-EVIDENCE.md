# V170-3.6 — Landing Page Local UAT Evidence

Date: 2026-09-20

Branch:

`feature/andy-core-v1.7.0-foundation`

Implementation head:

`2f5ea4c77f38c7c52a68a68812184808a4c967a0`

## Runtime alignment

The Local plugin runtime was overlaid from the v1.7.0 Foundation branch using only Git-tracked runtime files.

- tracked runtime files: 133
- SHA256 mismatches between worktree and Local plugin: 0
- Production: untouched

## Existing-site adoption fixture

Before loading the v1.7.0 runtime, Local had an explicit module option containing:

- inquiry_os
- email_os
- project_studio
- docs_os

Social Login and Connector were already disabled.

After v1.7.0 adoption:

- inquiry_os
- email_os
- project_studio
- docs_os
- landing_pages

No previously disabled module was re-enabled.

Persisted system markers:

- `yby_core_module_adoptions_v1 = [v170_landing_pages]`
- `yby_landing_page_rewrite_version = 1`

## CPT runtime

WordPress runtime assertions:

- post type: `yby_landing_page`
- show UI: true
- show in REST: true
- archive: false
- rewrite slug: `lp`
- rewrite version: `1`

## Temporary Landing Page lifecycle

A temporary Local-only Landing Page was created through WordPress APIs:

- temporary post ID: `39918`
- slug: `uat-v170-landing-page-recovery`

Generated permalink:

`http://localdev.beyourlover.com/lp/uat-v170-landing-page-recovery/`

The scheme reflects the current Local `home_url` configuration; the canonical path contract is:

`/lp/uat-v170-landing-page-recovery/`

The temporary post was permanently removed after validation.

Post-cleanup count:

`yby_landing_page = 0`

## Rewrite table

Persisted WordPress rewrite rules include the canonical singular route:

`lp/([^/]+)(?:/([0-9]+))?/?$ => index.php?yby_landing_page=$matches[1]&page=$matches[2]`

WordPress also generated the expected subordinate embed/trackback/pagination rules.

## Cleanup

After cleanup:

- temporary Landing Page posts: 0
- no temporary business content retained
- system module adoption marker retained
- system rewrite version marker retained
- Landing Pages remains enabled for continued v1.7.0 Local development

## Result

**PASS — Landing Page CPT Recovery Local lifecycle**

Validated:

- existing-site module adoption
- CPT registration
- /lp/{slug} permalink generation
- persisted rewrite rules
- temporary content creation
- temporary content cleanup
- preservation of previously disabled modules
