# Rollback Guide

If RC2 must be rolled back:

1. Restore the prior RC1 plugin ZIP.
2. Reactivate the prior plugin build.
3. Confirm the REST endpoint still returns the governed lead response.
4. Recheck Notification Center settings if the database was changed during validation.

